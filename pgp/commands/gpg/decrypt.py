class Command(object):

    multifile = True

    def __init__(self, io_helper, keyring_helper, skip_hidden_recipients,
                 use_embedded_filename, allow_multiple_messages,
                 override_session_key):
        pass

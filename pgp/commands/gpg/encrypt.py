class Command(object):

    def __init__(self, io_helper, force_mdc, disable_mdc, message_filename,
                 for_your_eyes_only, throw_keys, escape_from_lines,
                 set_filesize, show_session_key, override_session_key):
        pass
